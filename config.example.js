// Copy to config.js and add your Anthropic API key (or use the in-app Settings modal,
// which stores the key in localStorage and takes precedence). proxyUrl is optional
// and points to a server-side demo proxy such as a Cloudflare Worker route.
// NEVER commit a real key.
const CONFIG = { apiKey: "", proxyUrl: "" };
